# 소리누리 릴리스 체크리스트

매 배포 전 아래 항목을 순서대로 확인하세요.
모든 항목을 통과해야 프로덕션 배포(promote)를 진행할 수 있습니다.

---

## 1단계 — 코드 준비

- [ ] `python3 scripts/bump_version.py <버전>` 실행으로 버전 일괄 변경
- [ ] `RELEASE_NOTES.md` 최신 버전 섹션 업데이트
- [ ] 변경된 파일 전체 커밋 (`git add -A && git commit`)
- [ ] `main` 브랜치에 푸시 (`git push origin main`)

## 2단계 — 빌드 확인

- [ ] GitHub Actions 빌드 성공 확인 (초록색 체크)
- [ ] 버전 일치 검증 스텝 통과 확인
- [ ] 빌드 로그에서 컴파일 경고 없음 확인

## 3단계 — 스테이징 테스트

새 설치파일(`Sorinuri-Setup-<버전>.exe`)을 직접 설치하고 아래 항목을 확인합니다.

### 핵심 기능 회귀 테스트 (필수)

- [ ] **파일 열기** → 즉시 재생 시작 (방향키 없이)
- [ ] **타임라인 클릭** → 해당 위치로 즉시 이동
- [ ] **방향키** → 5초 시크 정상 동작
- [ ] **스페이스바** → 일시정지/재생 토글
- [ ] **마우스 휠** → 볼륨 ±5% 조절
- [ ] **더블클릭** → 전체화면 전환/해제
- [ ] **컨트롤바** → 3초 비활동 후 자동 숨김
- [ ] **마우스 커서** → 3초 비활동 후 자동 숨김
- [ ] **우클릭 메뉴** → 정상 표시
- [ ] **설정 창** → 열기/닫기 정상 동작
- [ ] **자동 업데이트 팝업** → 버전 번호 일치 확인 (팝업이 뜨지 않아야 함)

### 신규 기능 테스트 (해당 버전에 추가된 기능)

- [ ] (버전별로 추가)

## 4단계 — 프로덕션 확정

모든 테스트 통과 후에만 진행합니다.

- [ ] GitHub Actions → **Promote to Production** 워크플로우 실행
  - 버전 입력: `<버전>`
  - 확인 입력: `yes`
- [ ] 기존 사용자 PC에서 업데이트 팝업 정상 표시 확인
- [ ] 업데이트 설치 후 팝업이 다시 뜨지 않는지 확인

---

## 긴급 롤백 절차

문제 발생 시 즉시 실행합니다.

1. GitHub Actions → **Rollback to Previous Version** 워크플로우 실행
2. 롤백할 버전 입력 (예: `5.1.0`)
3. 확인 입력: `yes`
4. 완료 후 사용자에게 공지

---

## 워크플로우 목록

| 워크플로우 | 용도 | 실행 방식 |
| --- | --- | --- |
| `build-windows.yml` | 빌드 + 스테이징 배포 | 자동 (push 시) |
| `promote.yml` | 스테이징 → 프로덕션 확정 | 수동 (테스트 후) |
| `rollback.yml` | 이전 버전으로 긴급 롤백 | 수동 (문제 발생 시) |

---

## 버전 번호 변경 방법

```bash
# 세 곳(CMakeLists.txt, UpdateChecker.h, build-windows.yml)을 한 번에 변경
python3 scripts/bump_version.py 6.1.0
```

버전 불일치 상태로 푸시하면 빌드 단계에서 자동으로 실패합니다.
